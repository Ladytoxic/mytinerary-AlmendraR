import React from 'react'

const routes = [
  { path: '/', component: HomePage },
  { path: '**', component: ErrorPage }
]

const App = () => {
  return (
    <div>

    </div>
  )
}

export default App
